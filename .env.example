# Database
DATABASE_URL=postgresql://arnievulnai:localdev@localhost:5432/arnievulnai
PGHOST=localhost
PGPORT=5432
PGUSER=arnievulnai
PGPASSWORD=localdev
PGDATABASE=arnievulnai

# AWS — use IAM roles in production; keys only for local dev
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Anthropic AI (via Replit AI Integrations proxy)
AI_INTEGRATIONS_ANTHROPIC_BASE_URL=
AI_INTEGRATIONS_ANTHROPIC_API_KEY=

# VirusTotal (optional — also fetched from Secrets Manager at runtime)
VIRUSTOTAL_API_KEY=

# Frontend (set at build time)
VITE_API_BASE_URL=http://localhost:8080
